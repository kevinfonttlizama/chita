source "https://rubygems.org" #source of official ruby gems 

gem "httparty" #gem for requests , similar to axios(js) or requests (python) libraries 


#run bundle install or gem install to use the lib